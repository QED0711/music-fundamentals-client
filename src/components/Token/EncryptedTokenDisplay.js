import React from 'react';

const EncryptedTokenDisplay = (token) => {

    return(
        <div id="encrypted-token-display">
            <h3>{token}</h3>
        </div>
    )

}

export default EncryptedTokenDisplay;